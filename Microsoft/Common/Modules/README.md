# Common Modules

Use this folder for modules shared across providers (Azure, Entra, and future providers).

Examples: logging helpers, retry wrappers, CSV export helpers, and shared validation utilities.
